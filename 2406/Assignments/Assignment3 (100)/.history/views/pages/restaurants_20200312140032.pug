html 
    head
        title  You Want Food? 
body
    include ../partials/header
    
    h1 Restaurant Pages
    each restaurant in restaurantNames
        a(href="/restaurant/#{restaurant.id}" class = "links") #{restaurant.name}
